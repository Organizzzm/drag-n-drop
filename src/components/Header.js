import React from "react";

// app component
export default class Header extends React.Component {
    // render
    render() {
        return (
            <header className="header text-center clearfix">
                <h1 className="header__title">Drag and Drop like google disk</h1>
            </header>
        );
    }
}
